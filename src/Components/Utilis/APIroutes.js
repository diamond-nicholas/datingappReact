/* eslint-disable no-unused-vars */
const APIroutes = () => {
  const host = 'http://localhost:8001';

  const registerRoute = `${host}/api/v1/users/get`;
  const loginRoute = `${host}/api/v1/users/signin`;
};

export default APIroutes;
